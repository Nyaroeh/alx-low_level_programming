#include <stdio.h>
/**
 * main - a program that prints the alphabet in lowercase
 * Return: 0 is a success
 */
int main(void)
{
	for (i 'a' : i <= 'z' : i++)
		putchar(i);
	putchar("/n");
	return (0);
}
